export type RequestBook = {
    name: string,
    author: string,
    imageUrl: string,
    backgroundColor: string,
    report: string,
}
export type BookType = {
    id: string;
    name: string;
    imageUrl: string;
    author: string;
    rating: number;
    report: string;
    backgroundColor: string
}